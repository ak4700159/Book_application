// 옵션 클래스는 전역적으로 사용되며 사용자마다의 요구사항을
// 충족시키기 위해 필요한 속성들을 넣는다.
// Book application에서는 필요한 부분이 뭐가 있을지 생각해봐야 된다.

class Option {
  Option();
  // bool isLogin = false ; 자동 로그인
  // bool darkMode = false;
  // bool
}
